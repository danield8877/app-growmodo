# Build / dev — URL de l’API (sans slash final). Doit pointer vers la MÊME base que celle derrière Coolify
# (plus d’appel à un ancien domaine type api.growmodo.fr).
#
# Ex. https://api.amen-design.fr
# Dev local (Vite proxy /api + /uploads) :
VITE_API_URL=http://localhost:3001
